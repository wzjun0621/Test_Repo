#include <stdio.h>

int main() {
    int dan = 5; // 곱해지는 수
    int i; // 곱하는 수

    for (i = 1; i <= 9; i++) { // 곱해지는 수(dan)에 1부터 9까지 곱하는 식 출력
        printf("%d * %d = %d\n", dan, i, dan * i);
    }

    return 0;
}
